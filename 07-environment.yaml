apiVersion: v1
kind: Pod
metadata:
  name: environment
  labels:
    project: roboshop
    environment: dev
  annotations:
    jenkins: https://dev-build/jenkins/job
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
     requests:
        memory: "64Mi"
        cpu: "100m"
     limits:
        memory: "124Mi"
        cpu: "150m"
    env:
      - name: course
        value: "devops"
      - name: trainer
        value: "siva"

