apiVersion: v1
kind: Pod
metadata:
  name: resoucelimit
  labels:
    project: roboshop
    dev: environment
spec:
  containers:
  - name: nginx
    image: nginx
    resources:
      requests:
        memory: "68Mi"
        cpu: "100m"
      limits:
        memory: "128Mi"
        cpu: "150m"

