apiVersion: v1
kind: Pod
metadata:
  name: annotations
  labels:
    purpose: demo
    environment: dev
    project: devops
  annotations:
    purpose: anotations
    buildurl: https://buildurl
    descreption: This is demo for the anotations
spec:
  containers:
  - name: nginx
    image: nginx